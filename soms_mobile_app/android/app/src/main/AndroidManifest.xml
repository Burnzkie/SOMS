<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Deploy-readiness fix (Sep 2026): the debug/profile manifest overlays
         (src/debug, src/profile) already declare INTERNET, which is why
         `flutter run` and wireless-ADB testing worked fine, but a real
         `flutter build apk` (release) uses ONLY this manifest, and without
         this line a release build has zero network access, silently.
         CAMERA is needed for mobile_scanner (officer QR attendance scan);
         declaring it explicitly here too rather than relying on the
         plugin's manifest merge. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- In-app auto-update (10-Mobile-Deployment.md Part B): needed to
         trigger the package installer on the APK UpdateService downloads.
         This is a "special" permission — declaring it here only makes it
         requestable; the user still approves it via the OS's own
         "Allow SOMS to install unknown apps" settings screen, which
         UpdateService.openInstallPermissionSettings() opens. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <application
        android:label="soms_mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
